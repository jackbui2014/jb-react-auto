import React, {Fragment, Component} from 'react';

import './{{ name }}.scss';

const {{ name }} =(props)=>{
	return (
		<Fragment>
			{{name}} content here... <hr/>
		</Fragment>
	);
}
export default {{ name }};