import React, {Fragment, Component} from 'react';
import {globalSettings} from '../../config/config.js';
import './{{name}}.scss';
const Header = React.lazy(() => import('../../layouts/'+globalSettings.layout+'/Header/Header'));
const Footer = React.lazy(() => import('../../layouts/'+globalSettings.layout+'/Footer/Footer'));
const {{name}}Content = React.lazy(() => import('../../layouts/'+globalSettings.layout+'/Body/{{name}}Content/{{name}}Content.jsx'));
const {{name}} =(props)=>{
	return (
		<Fragment>
			<Header />
			<{{name}}Content />
			<Footer />	
		</Fragment>
	);
}
export default {{name}};